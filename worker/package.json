{
  "name": "behest-worker",
  "version": "0.1.0",
  "private": true,
  "description": "behest credential relay broker",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc --noEmit",
    "test": "tsc --noEmit"
  },
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250405.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
